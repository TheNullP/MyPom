services:
  app-mypom:
    build: .
    container_name: mypom-container
    restart: on-failure

    networks:
      - web-network
    ports:
      - '127.0.0.1:8080:8080'
    env_file:
        - .env

networks:
  web-network:
    external: true
