services:
  app-mypom:
    build: .
    container_name: mypom-container
    restart: on-failure

    depends_on:
      - postgres
    networks:
      - web-network
    ports:
      - '8080:8080'
    env_file:
        - .env

networks:
  web-network:
